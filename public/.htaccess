Options -Indexes
RewriteEngine On

# Cho phép truy cập file tĩnh
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Các request khác đẩy về index.php
RewriteRule ^ index.php [QSA,L]
